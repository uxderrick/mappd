{
  "private": true,
  "scripts": {
    "build": "cd canvas && npm run build && cp -r dist ../cli/canvas-dist",
    "build:canvas": "cd canvas && npm run build",
    "build:cli": "cd cli && npm run build"
  }
}
